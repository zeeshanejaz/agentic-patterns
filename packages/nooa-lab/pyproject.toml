[project]
name = "sd-agentic-nooa"
version = "0.1.0"
requires-python = ">=3.12"
license = { text = "MIT" }
dependencies = [
    "sd-agentic-shared",
    "nooa",
    "opentelemetry-exporter-otlp-proto-http>=1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
sd-agentic-shared = { workspace = true }
