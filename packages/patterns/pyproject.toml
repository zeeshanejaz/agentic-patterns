[project]
name = "sd-agentic-patterns"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "sd-agentic-shared",
    "openai>=1",
    "langfuse>=3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
sd-agentic-shared = { workspace = true }
