## Why

Prioritization (#20) is next. Score a support inbox, handle the top tickets, re-score what is left so urgency can change.

## What Changes

- Add `sd_agentic_patterns.prioritization` over `SAMPLE_EMAILS`. Score → execute top → re-score leftover. Tags `pattern:prioritization`. README scratch shipped.

## Capabilities

### New Capabilities

- `prioritization-scratch`: From-scratch inbox scoring, execute-top, re-score.

### Modified Capabilities

- (none)

## Impact

- New module, score prompt, README. No ports.
